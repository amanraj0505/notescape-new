import React from 'react';
import {Text, View} from 'react-native';
function MainHeader() {
  return (
    <View
      style={{
        backgroundColor: 'red',
        flex: 1,
        height: '100%',
        width: '100%',
      }}></View>
  );
}

export default MainHeader;
